export enum DESTINATIONS {
  SELECT_WHERE = "SELECT_WHERE",
  SELECT_FROM = "SELECT_FROM",
}

export enum SEARCH_FIELD_NAMES {
  FROM = "from",
  WHERE = "where",
  DATE = "date",
}

export const SKELETON_MIN_HEIGHT = "56px";
